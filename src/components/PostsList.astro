---
import { getCollection } from "astro:content";
import PostsListItem from "./PostsListItem.astro";

const allPosts = await getCollection("posts");
const posts = allPosts;
---

{posts.map((post) => <PostsListItem post={post.data} />)}
